<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Orchestration.App.Views.TerminalNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <Grid x:Name="NodeRoot"
          PointerEntered="OnNodePointerEntered"
          PointerExited="OnNodePointerExited">
        <Border x:Name="NodeBorder"
                Background="{ThemeResource TetherNodeBrush}"
                BorderBrush="{ThemeResource TetherBorderBrush}"
                BorderThickness="1"
                CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="HeaderRow" Height="44" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid x:Name="HeaderBar"
                      Background="{ThemeResource TetherNodeHeaderBrush}">
                    <Grid x:Name="HeaderContent"
                      Height="44"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Padding="12,0"
                      ColumnSpacing="9">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Ellipse x:Name="StateDot"
                             Width="8"
                             Height="8"
                             Fill="{StaticResource TetherAccentVioletBrush}"
                             VerticalAlignment="Center" />
                    <Border Grid.Column="1"
                            Padding="6,2"
                            CornerRadius="4"
                            Background="{StaticResource TetherBadgeBrush}"
                            VerticalAlignment="Center">
                        <TextBlock x:Name="KindText"
                                   Text="TERMINAL"
                                   Foreground="White"
                                   FontFamily="Segoe UI Variable Text"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   CharacterSpacing="70" />
                    </Border>
                    <StackPanel Grid.Column="2"
                                VerticalAlignment="Center"
                                Spacing="-1">
                        <TextBlock x:Name="TitleText"
                                   Text="terminal"
                                   Foreground="{ThemeResource TetherTextPrimaryBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   FontFamily="Segoe UI Variable Text"
                                   FontSize="12"
                                   FontWeight="SemiBold" />
                        <TextBlock x:Name="ProjectText"
                                   Foreground="{ThemeResource TetherTextMutedBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   FontFamily="Segoe UI Variable Text"
                                   FontSize="10" />
                    </StackPanel>

                    <StackPanel Grid.Column="3"
                                Orientation="Horizontal"
                                Spacing="2">
                        <Button x:Name="RestartButton"
                                Style="{StaticResource TetherNodeActionButtonStyle}"
                                Click="OnRestart"
                                ToolTipService.ToolTip="Reiniciar">
                            <FontIcon Glyph="&#xE72C;" FontSize="12" />
                        </Button>
                        <Button x:Name="KillButton"
                                Style="{StaticResource TetherDangerNodeActionButtonStyle}"
                                Click="OnKill"
                                ToolTipService.ToolTip="Encerrar processo">
                            <FontIcon Glyph="&#xE711;" FontSize="12" />
                        </Button>
                        <Button x:Name="CloseButton"
                                Style="{StaticResource TetherDangerNodeActionButtonStyle}"
                                Click="OnClose"
                                ToolTipService.ToolTip="Remover nó">
                            <FontIcon Glyph="&#xE74D;" FontSize="12" />
                        </Button>
                    </StackPanel>
                    </Grid>
                </Grid>

                <Grid Grid.Row="1">
                    <controls:WebView2 x:Name="Web" DefaultBackgroundColor="#0D0A13" />
                    <Border x:Name="ExitOverlay"
                            Visibility="Collapsed"
                            Background="{ThemeResource TetherOverlayBrush}"
                            Padding="16">
                        <StackPanel VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Spacing="12">
                            <TextBlock x:Name="ExitText"
                                       Foreground="{StaticResource TetherDangerBrush}"
                                       TextAlignment="Center"
                                       FontFamily="Segoe UI Variable Text"
                                       FontSize="12" />
                            <Button Content="Reiniciar"
                                    Style="{StaticResource TetherToolbarButtonStyle}"
                                    Click="OnRestart"
                                    HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>
        <Border x:Name="HoverRing"
                Margin="-2"
                BorderBrush="{StaticResource TetherAccentVioletBrush}"
                BorderThickness="1"
                CornerRadius="14"
                Opacity="0"
                IsHitTestVisible="False" />
        <Border x:Name="SelectionRing"
                Margin="-2"
                BorderBrush="{StaticResource TetherAccentLimeBrush}"
                BorderThickness="2"
                CornerRadius="14"
                Opacity="0"
                IsHitTestVisible="False" />
        <Border x:Name="ResizeGripElement" Width="20" Height="20"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Background="Transparent"
                ToolTipService.ToolTip="Redimensionar" />
        <Border x:Name="ConnectionSurfaceElement"
                Visibility="Collapsed"
                Background="Transparent"
                ToolTipService.ToolTip="Arraste para conectar" />
    </Grid>
</UserControl>
