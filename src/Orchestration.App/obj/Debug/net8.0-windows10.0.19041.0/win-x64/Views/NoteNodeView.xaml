<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Orchestration.App.Views.NoteNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid x:ConnectionId='2' x:Name="HeaderBar"
                  Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
                  Padding="10,6"
                  ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="nota" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold" />

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="2">
                    <Button x:ConnectionId='4'                 Padding="6,2" Background="Transparent"
                            BorderThickness="0" ToolTipService.ToolTip="Remover no">
                        <FontIcon Glyph="&#xE74D;" FontSize="12" />
                    </Button>
                </StackPanel>
            </Grid>

            <TextBox x:ConnectionId='3' x:Name="Editor"
                     Grid.Row="1"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     BorderThickness="0"
                     Background="Transparent"
                     PlaceholderText="Escreva em markdown..."
                     FontFamily="Cascadia Mono, Consolas"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Border>
</UserControl>

